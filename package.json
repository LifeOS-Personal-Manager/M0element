{
  "name": "m0element",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx serve .",
    "check": "node -e \"const fs=require('fs');const html=fs.readFileSync('index.html','utf8');[...html.matchAll(/<script(?![^>]*src=)[^>]*>([\\s\\S]*?)<\\/script>/gi)].forEach((m,i)=>{new Function(m[1]);console.log('script '+(i+1)+' OK')})\""
  },
  "devDependencies": {}
}
